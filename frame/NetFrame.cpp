#include "NetFrame.h"
#include "AsyncNet.h"

using namespace iak;

AsyncNet::init(uint32_t count) {
	AsyncNet::init(count);
}

AsyncNet::destroy() {
	AsyncNet::destroy();
}

void AsyncNet::dispatch() {
	AsyncNet::dispatch();
}